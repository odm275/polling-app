import React, { Component } from 'react';

class MyPolls extends Component {
    render(){
        return(
            <div>
                Here same, some fetch() action that goes into redux and gets myPolls from server.
            </div>
        );
    }
}

export default MyPolls;